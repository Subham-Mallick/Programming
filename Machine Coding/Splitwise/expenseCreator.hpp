#include "expenseEnum.hpp"
#include "expense.hpp"
#include "expenseInfo.hpp"

class ExpenseCreator
{
    public:
        Expense createExpense(ExpenseType expenseType,double expenseAmmount,User paidBy,vector<Split> expenseSplit,ExpenseInfo expenseInfo)
        {
            if(expenseType == EQUAL)
        }
};